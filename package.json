{
  "name": "agent-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal zero-dependency demo app wired up with GitHub Spec Kit (spec-driven development)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
